# Database Connection
# Connect to PostgreSQL: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://er_user:secure_password@localhost:5432/er_review_prod"

# Authentication
# Generate a secure random string for JWT encryption (openssl rand -base64 32)
JWT_SECRET="generate_a_secure_random_secret_string_here"

# App Environment
NODE_ENV="production"

# Optional: External API Base URL if needed
# API_BASE_URL="https://api.example.com"
